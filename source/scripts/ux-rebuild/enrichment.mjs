import {curriculum} from './curriculum.mjs';
import {depthHtml} from './course-depth.mjs';
import {lessonUrl} from './course-lesson-routes.mjs';
import {marketing,courseScreens,brochureRoutes,brochureName,courseGate} from './marketing.mjs';
export function courseLessonLinks(c,{icon,link}){
 return `<a class="button" href="${lessonUrl(c.slug)}">${icon('ArrowRight')}Go to lesson</a>`;
}
export function richProgram(c,h){const {icon,esc,link,checks,card,modules,tech}=h,m=marketing[c.slug],free=c.pricingCategory==='FREE';return `<section class="rich-hero" data-curriculum-version="${curriculum(c).version}" style="--course-accent:${m.accent}"><div class="rich-copy"><span class="eyebrow">${esc(c.category)} · ${esc(c.level)}</span><h1>${esc(c.title)}</h1><h2>${m.headline}</h2><p>${esc(c.description)}</p>${tech(c.tools)}<div class="actions"><a class="button primary" href="${courseGate(c)}" download>${icon('Download')}Get the course brochure</a>${courseLessonLinks(c,{icon,link})}</div><div class="facts"><span>${icon('Clock3')}${esc(c.durationText)}</span><span>${icon('Layers')}${c.modules.length} modules</span><span>${icon(free?'Gift':'GraduationCap')}${free?'Free · full access':'Professional program'}</span></div></div><figure><img src="../assets/course-visuals/${m.image}" alt="${esc(c.title)} visual concept"><figcaption>${icon(m.icon)}${m.project}</figcaption></figure></section>${c.slug==='entrepreneurship'?`<section class="card section"><h2>${icon('BookOpen')}Learn entrepreneurship. Apply separately.</h2><p class="section">Study all seven modules and complete course assignments without submitting an idea. Venture evaluation, admission and funding are separate decisions.</p><a class="button section" href="010.html">${icon('Lightbulb')}Submit an idea for venture evaluation</a></section>`:''}<nav class="tabs section"><a class="active" href="#roadmap">Learning roadmap</a><a href="#outcomes">Outcomes</a><a href="#curriculum">Curriculum</a><a href="#projects">Projects & tools</a><a href="#teaching-example">Sample lesson</a><a href="#delivery">Delivery</a></nav><section id="roadmap" class="card section"><div class="cardhead"><h2>${icon('Route')}Your route from concept to evidence</h2><span class="meta">Explore the connected curriculum below</span></div><div class="course-roadmap">${m.stages.map((t,i)=>`<a href="#module-${Math.min(i,c.modules.length-1)}"><span>${icon(['Compass','Layers','Code2','Network','ShieldCheck','Rocket','TrendingUp'][i])}</span><small>0${i+1}</small><b>${t}</b>${icon('ArrowRight')}</a>`).join('')}</div></section><div class="grid2 section"><section class="card" id="outcomes"><h2>${icon('Target')}What you will be able to do</h2><div class="section">${checks(c.outcomes)}</div></section><section class="card"><h2>${icon('Users')}Find your starting point</h2><div class="grid2 section"><div><h3>Designed for</h3>${checks(c.audience)}</div><div><h3>Before you begin</h3>${checks(c.prerequisites)}</div></div></section></div><section class="card section" id="curriculum"><div class="cardhead"><h2>${icon('Layers')}Curriculum that builds on itself</h2><button class="button small" data-action="expand-modules">${icon('ChevronsDown')}Expand all modules</button></div><div class="curriculum-grid">${modules(c).replaceAll('<details class="module"',(()=>{let i=0;return ()=>`<details id="module-${i++}" class="module"`;})())}</div></section><section id="projects" class="project-story section"><div><span class="eyebrow">Build, test and explain</span><h2>${m.project}</h2><p>${m.brief}</p><div class="evidence-cards">${m.evidence.map((t,i)=>`<article>${icon(['FolderCode','ClipboardCheck','Presentation'][i])}<h3>${t}</h3></article>`).join('')}</div></div><img src="../assets/course-visuals/${c.slug}-roadmap.svg" alt="${esc(m.stages.join(' to '))}"></section>${modulePreview(c,esc,icon)}<div class="grid3 section" id="delivery">${card('Delivery & schedule',`<p>${esc(c.durationText)} · ${esc(c.deliveryMode.replaceAll('_',' ').toLowerCase())}</p><p class="section">Confirm the current schedule and session arrangements before enrollment.</p>`,'CalendarDays')}${card('Completion evidence',`<p>${c.requiresPassingExam?'Complete required coursework and pass the assessment.':'Complete the required course activities.'}</p><p class="section">${c.certificateEnabled?'Completion certificate available.':'Your course record captures your work.'} No placement or outcome guarantees.</p>`,'Award')}${card('Take the next step',`<p>Keep the full curriculum, outcomes and learning plan in one searchable PDF.</p><a class="button primary section" href="${courseGate(c)}" download>${icon('Download')}Download brochure</a>`,'BookOpen')}</div>`;}
export function brochureScreen(s,courses,h){const {icon,esc,link,heading}=h; if(s.n===158)return `${heading('Find your next chapter','All twelve courses. Download any PDF directly—no form, sign-in or payment required.',`<a class="button primary" href="../brochures/all-course-brochures.zip" download>${icon('Download')}Download all brochures (ZIP)</a>`)}<div class="program-grid">${courses.map(c=>`<article class="card program-card"><img class="catalog-photo" src="../assets/course-visuals/${marketing[c.slug].image}" alt="${esc(c.title)}"><div class="copy"><h2>${esc(c.title)}</h2><p>${marketing[c.slug].headline}</p><div class="facts"><span>${icon('FileText')}6-page PDF</span><span>${icon('Clock3')}${esc(c.durationText)}</span></div><a class="button primary" href="${courseGate(c)}" download>${icon('Download')}Download PDF</a></div></article>`).join('')}</div>`;
 const [slug,route]=Object.entries(brochureRoutes).find(([,ids])=>ids.includes(s.n))||['ai-engineering',[43,143]],c=courses.find(c=>c.slug===slug),m=marketing[slug],ready=s.n===route[1];return `${heading(ready?'Your course brochure is ready':'Get the complete course guide',c.title,link('All brochures',158,'LibraryBig'))}<div class="brochure-experience"><section class="brochure-preview"><img src="../assets/course-visuals/${m.image}" alt="${esc(c.title)} illustration"><div><span class="eyebrow">SamkhyaAcademy · Program guide</span><h2>${esc(c.title)}</h2><p>${m.headline}</p><div class="facts"><span>${icon('FileText')}PDF · 6 pages</span><span>${icon('Search')}Searchable text</span></div></div></section><section class="card">${ready?`<div class="download-ready">${icon('CircleCheck')}<h2>A useful guide for your next decision.</h2><p>Read the outcomes, curriculum, project brief and delivery information at your own pace.</p><a class="button primary" href="../brochures/${brochureName(c)}" download>${icon('Download')}Download PDF brochure</a><a class="button" href="../brochures/${brochureName(c)}" target="_blank" rel="noopener">${icon('ExternalLink')}Open PDF in a new tab</a>${link('Explore the course',courseScreens[slug],'ArrowRight')}<p class="meta">${brochureName(c)} · A4 portrait · No payment required</p></div>`:`<form data-form="brochure-lead" data-course="${slug}" data-next="${route[1]}"><h2>Your brochure is available now</h2><a class="button primary section" href="../brochures/${brochureName(c)}" download>${icon("Download")}Download PDF without entering details</a><p class="section">No form is required to download. You may optionally leave your details below to preview the contact-request experience.</p><div class="fields section"><label>Full name *<input name="name" autocomplete="name" required></label><label>Email address *<input name="email" type="email" autocomplete="email" required></label><label class="wide">Mobile number *<input name="mobile" type="tel" autocomplete="tel" pattern="[+0-9 ()-]{7,20}" required></label></div><label class="consent-row"><input type="checkbox" name="emailConsent">Email me optional course updates.</label><label class="consent-row"><input type="checkbox" name="whatsappConsent">Send me optional WhatsApp updates.</label><button type="submit" class="button primary">${icon('Download')}Get the PDF brochure</button><p class="meta section">Customer prototype: details are stored in this browser only. No email or WhatsApp message is sent.</p></form>`}</section></div><div class="grid4 section">${[['Positioning','Who the course is for','Compass'],['Learning outcomes','Capabilities you will develop','Target'],['Curriculum roadmap','Modules and practical topics','Route'],['Projects & next steps','Tools, delivery and completion','FolderCode']].map(([a,b,i])=>`<section class="card"><h3>${icon(i)}${a}</h3><p class="section">${b}</p></section>`).join('')}</div>`;
}
export function ventureExtension(s,h){const {icon,esc,link,card,checks,heading,row,callout}=h,n=s.n;let body='';const form=(kind,next,content,button)=>`<form class="card" data-form="${kind}" data-next="${next}">${content}<button type="submit" class="button primary section">${icon('Send')}${button}</button></form>`;
if(n===146)body=`<section class="investor-hero"><div><span class="eyebrow">Founders · Mentors · Investors</span><h1>Great conversations start with credible evidence.</h1><p>Explore the venture ecosystem, share an idea for program review, or request investor-network access.</p><div class="actions">${link('Submit your idea',10,'Lightbulb','button primary')}${link('I am an investor',147,'BriefcaseBusiness')}${link('Founder workspace',81,'LayoutDashboard')}</div></div><img src="../assets/workshop.png" alt="Founders discussing a venture with a mentor"></section><div class="grid3 section">${card('For aspiring founders',checks(['Submit a problem and solution hypothesis.','Receive a program review decision.','Begin the seven-stage journey if accepted.'])+link('Start idea submission',10,'ArrowRight'),'Lightbulb')}${card('For investors',checks(['Share your investment interests.','Request reviewed network access.','Ask for founder-approved introductions.'])+link('Create investor profile',147,'ArrowRight'),'BriefcaseBusiness')}${card('For active founders',checks(['Choose what the network can see.','Review each introduction request.','Keep private artifacts private.'])+link('Manage pitch visibility',152,'ShieldCheck'),'Rocket')}</div><div class="section cta"><div><h2>See how an introduction works</h2><p>Explore an illustrative, consented showcase. Network membership and funding are never guaranteed.</p></div>${link('Preview venture showcase',149,'Eye')}</div>`;
else if(n===147)body=`<div class="two-rail">${form('investor-application',148,`<h2>Tell us about your investment interests</h2><div class="fields section"><label>Full name *<input name="name" required></label><label>Work email *<input name="email" type="email" required></label><label>Organization *<input name="organization" required></label><label>Role<select name="role"><option>Venture capital investor</option><option>Angel investor</option><option>Corporate venture team</option></select></label><label>Focus sector<select name="sector"><option>Technology & AI</option><option>Climate & agriculture</option><option>Education</option><option>Sector agnostic</option></select></label><label>Preferred stage<select name="stage"><option>Pre-seed</option><option>Seed</option><option>Early growth</option></select></label><label class="wide">Profile or organization website *<input name="website" type="url" required placeholder="https://"></label><label class="wide">What would make an introduction useful?<textarea name="interests"></textarea></label></div><label class="consent-row"><input type="checkbox" required name="terms">I agree that introductions require founder approval and that private materials must not be redistributed.</label>`,'Request network access')}<aside class="stack">${card('A reviewed network',checks(['The program team reviews the profile.','Access is limited to founder-approved summaries.','Private decks require a separate permission decision.']),'ShieldCheck')}${callout('Prototype boundary','This form records a local demo request only. No investor is contacted and no due diligence or identity verification is performed.')}</aside></div>`;
else if(n===148)body=`${callout('Investor profile received','Request INV-2026-008 is awaiting program-team review. You do not yet have private venture access.')}<div class="grid2 section">${card('What happens next',row('Profile review','The program team checks your interests and affiliation.','ClipboardCheck')+row('Access decision','A separate approval is required before network access.','ShieldCheck')+row('Introductions','Founders decide whether to accept a request.','Handshake'),'Route')}${card('Explore while you wait',`<p>Use the public demo showcase to understand the experience. These are illustrative records, not live investment opportunities.</p><div class="section">${link('Preview showcase',149,'Eye')}</div>`,'Compass')}</div>`;
else if(n===149)body=`${callout('Consented summary preview','Illustrative opportunities for customer review. No private decks, customer data or financial projections are publicly exposed.')}<div class="grid3 section">${[['AgriPulse','Crop advisory for small farmers.','Customer validation','Sprout'],['LearnLoop','Practice-led learning for students.','MVP build','GraduationCap'],['CircularWorks','Repair and reuse workflow for local teams.','Pilot launch','Recycle']].map(([name,desc,stage,i])=>card(name,`<p>${desc}</p><div class="section">${row('Stage',stage,'Route')}${row('Sharing','Founder-approved public summary','ShieldCheck')}</div><p class="meta">Demo opportunity · ${name==='AgriPulse'?'Introduction available':'Not accepting introductions'}</p><div class="section">${name==='AgriPulse'?link('Request introduction',150,'Handshake','button primary'):'<button class="button" disabled>Introductions closed</button>'}</div>`,i)).join('')}</div>${card('What is not shared here',checks(['Private pitch decks and source files','Personal customer or interview details','Unapproved financial or investment claims']),'LockKeyhole')}`;
else if(n===150)body=`<div class="two-rail">${form('introduction',151,`<h2>Request an introduction to AgriPulse</h2><p class="section">Explain your interest. The founder can accept or decline before any contact details or private files are shared.</p><div class="fields section"><label>Your name *<input name="name" required></label><label>Work email *<input type="email" name="email" required></label><label class="wide">Why is this a useful fit? *<textarea name="reason" required></textarea></label></div><label class="consent-row"><input type="checkbox" required name="consent">I understand that this is an introduction request, not access to private documents or an investment commitment.</label>`,'Send introduction request')}<aside>${card('Founder-controlled sharing',row('AgriPulse','Customer validation stage','Sprout')+checks(['Request enters the founder review queue.','No private documents are attached.','The founder decides the next step.']),'ShieldCheck')}</aside></div>`;
else if(n===151)body=`${callout('Introduction request recorded','INTRO-2026-012 is awaiting the founder’s decision. Contact details and private artifacts have not been shared.')}<div class="grid2 section">${card('Follow the review flow',row('Requested','Your interest has been captured locally.','Send')+row('Founder decision','Accept or decline the request.','UserCheck')+row('Agreed next step','An introduction proceeds only with approval.','Handshake'),'Route')}${card('Preview the other side',`<p>For this customer demonstration, switch to the founder’s request inbox and see the decision controls.</p><div class="section">${link('Open founder inbox',157,'Inbox')}</div>`,'Users')}</div>`;
else if(n===152)body=form('pitch-sharing',157,`<h2>AgriPulse · Pitch visibility</h2><p class="section">Only the items you explicitly select belong in the network summary. Private artifacts stay locked.</p><div class="fields section"><label class="wide">Public summary<textarea name="summary">Crop advisory for small farmers, currently validating needs through customer interviews.</textarea></label><label>Stage<select name="stage"><option>Customer validation</option><option>MVP build</option><option>Pilot launch</option></select></label></div><label class="consent-row"><input type="checkbox" name="publishSummary">Allow the approved network to see this summary.</label><label class="consent-row"><input type="checkbox" name="acceptIntroductions">Accept introduction requests for founder review.</label>${callout('Always private by default','Pitch decks, financial models, interview notes and team contact details are not included. Each sharing request requires a separate decision.')}`,'Save sharing preferences');
else if(n===153)body=`${callout('Program review workspace','Demo role: admissions reviewer. Idea review is separate from course publishing and investor access.')}<section class="card section"><h2>Idea submissions awaiting review</h2><div class="table-wrap"><table><thead><tr><th>Idea</th><th>Applicant</th><th>Evidence</th><th>Status</th><th>Action</th></tr></thead><tbody><tr><td>AgriPulse</td><td>Arjun Nair</td><td>Problem statement + founder profile</td><td>Pending review</td><td>${link('Review submission',154,'ClipboardCheck')}</td></tr></tbody></table></div></section><div class="grid3 section">${card('Assess clarity',checks(['Problem specificity','Audience relevance','Evidence and assumptions']),'Lightbulb')}${card('Decide responsibly',checks(['Explain your decision','Request missing information','Keep an auditable review trail']),'ShieldCheck')}${card('Accepted ideas',`<p>Acceptance opens the first stage. It does not grant funding, investor access or automatic approval of later stages.</p>`,'Rocket')}</div>`;
else if(n===154)body=`<div class="two-rail"><div class="stack">${card('AgriPulse · Submitted idea',row('Problem','Small farmers need timely, locally relevant crop advice.','Lightbulb')+row('Audience','Smallholder farmers and local advisory groups.','Users')+row('Proposed solution','A guided advisory workflow informed by field interviews.','Sprout')+row('Evidence status','Initial hypothesis; further validation required.','ClipboardCheck'),'FileText')}${card('Review note',`<label>Feedback to applicant<textarea id="idea-review-note">The problem and audience are clear enough to begin structured validation. Test the key assumptions with prospective users before building an MVP.</textarea></label><div class="actions section"><button class="button primary" data-action="accept-idea">${icon('CircleCheck')}Accept into Stage 1</button>${link('Request changes',156,'Undo2')}</div><p class="meta section">The decision is stored locally for this prototype.</p>`,'MessageSquare')}</div><aside>${card('Decision checklist',checks(['Check the problem and audience.','Explain any missing evidence.','Do not imply funding or selection guarantees.','Acceptance unlocks only Stage 1.']),'ShieldCheck')}</aside></div>`;
else if(n===155)body=`${callout('AgriPulse accepted into Stage 1','Idea & Opportunity is now available in this demo. Complete its requirements before requesting the next stage.')}<div class="grid2 section">${card('Your first stage',checks(['Refine the problem statement.','Document the intended audience.','List assumptions and supporting evidence.'])+link('Open founder workspace',181,'Rocket','button primary'),'Lightbulb')}${card('Review history',row('Submitted','Arjun Nair · IDEA-2026-0142','Send')+row('Reviewed and accepted','Program reviewer · local demo decision','ClipboardCheck')+row('Stage unlocked','Stage 1 only; later stages remain gated.','LockKeyhole'),'History')}</div>`;
else if(n===156)body=`${callout('Changes requested','Clarify the target segment and add evidence about the urgency of the problem. Your original idea is preserved.','orange')}${form('idea-resubmission',74,`<h2>Revise your idea</h2><div class="fields section"><label class="wide">Updated audience *<textarea name="audience" required></textarea></label><label class="wide">Evidence and response to feedback *<textarea name="response" required></textarea></label></div>`,'Resubmit for review')}`;
else if(n===157)body=`<section class="card"><div class="cardhead"><h2>${icon('Inbox')}AgriPulse · Introduction requests</h2>${link('Sharing preferences',152,'SlidersHorizontal')}</div><p>Demo investor profile · Technology & agriculture · INTRO-2026-012</p><div class="callout purple section"><b>Reason for introduction</b><p>We would like to understand the customer-validation work and discuss whether our experience could be useful.</p></div><div class="actions section"><button class="button primary" data-action="accept-introduction">${icon('Handshake')}Accept introduction</button><button class="button" data-action="decline-introduction">${icon('X')}Decline request</button></div><p id="introduction-status" class="section" role="status">Pending founder decision. No files or contact details have been shared.</p></section><div class="grid2 section">${card('Control what follows',checks(['Acceptance records intent to connect.','Document access requires separate permission.','A request can be declined without sharing anything.']),'ShieldCheck')}${card('Your private library',`<p>Your artifacts stay in the founder workspace, independent of introduction decisions.</p><div class="section">${link('Review artifacts',26,'FolderLock')}</div>`,'LockKeyhole')}</div>`;
if(n===181||n===182)body=`${callout(n===182?'Stage 1 evidence submitted':'AgriPulse · Stage 1: Idea & Opportunity',n===182?'Your problem evidence is awaiting mentor review. Stage 2 remains locked.':'Turn your accepted idea into a clear problem statement and evidence plan.')}<div class="grid2 section">${card('Stage checklist',`<label class="consent-row"><input type="checkbox" ${n===182?'checked':''}>Define the problem and affected audience.</label><label class="consent-row"><input type="checkbox" ${n===182?'checked':''}>Record initial evidence and assumptions.</label><label class="consent-row"><input type="checkbox" ${n===182?'checked':''}>Explain the opportunity and proposed next test.</label>`,'ListChecks')}${card('Required artifact',n===182?row('Problem statement v1.0','Submitted for mentor review · local prototype','FileCheck'):`<h3>Problem statement and evidence plan</h3><p class="section">Attach a local file for this demo. No server upload takes place.</p><input class="section" type="file" data-upload aria-label="Stage 1 problem statement"><p id="upload-status" role="status" class="section"></p>`,'FolderOpen')}</div><div class="section cta"><div><h2>${n===182?'Mentor decision pending':'Ready to request a review?'}</h2><p>${n===182?'An independent review is required before the next stage opens.':'Check your problem evidence before submitting.'}</p></div>${link(n===182?'Back to Stage 1':'Submit demo evidence',n===182?181:182,n===182?'ArrowLeft':'Send')}</div>`;
return `${n===146?'':heading(s.name,'Venture Builder · Connected, permission-aware customer prototype',link('Venture & investor hub',146,'Compass'))}${body}`;
}
function modulePreview(c,esc,icon){const mod=curriculum(c).modules[0],lesson=mod.lessons[0];return `<section class="card section" id="teaching-example"><span class="eyebrow">Inside module 1 · ${esc(mod.title)}</span><h2>${icon('BookOpen')}${esc(lesson.title)}</h2><p class="section">${esc(lesson.explanation)}</p><p class="section"><b>Module assignment:</b> ${esc(mod.evidence)}</p><a class="button section" href="${lesson.file}">${icon('ArrowRight')}Open this lesson</a></section>`;}

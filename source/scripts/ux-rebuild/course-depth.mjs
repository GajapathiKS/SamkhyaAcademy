// Proposed teaching samples for UX review; not a claim that every lesson is authored.
export const courseDepth={
 'c-programming-fundamentals':{topic:'Compute a sensor average without losing precision',scenario:'A temperature logger stores integer readings, but the average may be fractional. You need a reusable function that keeps the input unchanged and handles an empty reading set.',explanation:'An array argument does not carry its element count into a C function. Pass the count separately and use size_t for indexing. Accumulate into a double so the final division is floating-point rather than integer division. Check the count before dividing; the caller must distinguish missing data from an actual zero reading.',example:'int readings[] = {21, 24, 26};\nsize_t count = sizeof readings / sizeof readings[0];\ndouble total = 0.0;\nfor (size_t i = 0; i < count; ++i) {\n    total += readings[i];\n}\nif (count > 0) printf("%.2f\\n", total / count);',result:'The three readings total 71. Dividing by 3 produces 23.67 when formatted to two decimal places. A loop condition of i <= count would access an element outside the array.',tasks:['Extract the calculation into a function with an explicit empty-input policy.','Add minimum and maximum readings without modifying the array.','Read values from a local text fixture and report malformed lines.'],criteria:['Correct average for positive and negative readings','No division by zero or out-of-bounds access','Tests for one reading, repeated readings and no readings'],question:'Why is total initialized as double rather than int?',answer:'It preserves a fractional result during division and avoids integer truncation in the averaging expression.'},
 'cpp-essentials':{topic:'Represent ownership with values and containers',scenario:'A study-planner application needs a list of tasks. Each task has a title and a completion flag. Users can add tasks while the program is running.',explanation:'std::vector owns its elements and manages its allocated storage. Prefer this value-based model to manually allocating each task. Iterate with const references when you only need to inspect tasks. Growing a vector may reallocate its storage, so do not keep references to elements across an insertion that might invalidate them.',example:'struct Task { std::string title; bool done; };\nstd::vector<Task> tasks{{"Review vectors", false}};\ntasks.push_back({"Write tests", true});\nfor (const auto& task : tasks) {\n    std::cout << task.title << ": " << task.done << "\\n";\n}',result:'The vector contains two independently owned task values. The range loop reads them without copying each task. The container releases its storage when its lifetime ends.',tasks:['Add a function that counts completed tasks.','Implement lookup by a stable task ID instead of a saved element reference.','Save and load task records with error handling for malformed input.'],criteria:['Clear ownership without raw owning pointers','Correct empty-list and missing-ID behavior','Tests showing state survives save and reload'],question:'Why might a saved reference become invalid after push_back?',answer:'If the vector reallocates, its elements move to new storage and references into the old storage are invalidated.'},
 'javascript-foundations':{topic:'Build a predictable filter interaction',scenario:'A course list should update as a learner types. The original course data must remain available so clearing the search restores every item.',explanation:'Keep source data separate from the rendered result. Array.filter creates a new selection rather than removing elements from the original array. Normalize both the query and the title before comparison. Render user-derived text with textContent so a query is treated as data rather than HTML.',example:'const courses = ["C Fundamentals", "JavaScript", "Full Stack"];\nconst query = "script".trim().toLowerCase();\nconst visible = courses.filter(title =>\n  title.toLowerCase().includes(query)\n);\noutput.textContent = visible.join(", ");',result:'The result is JavaScript. Clearing the query matches every title. A separate no-results message can explain an empty selection without altering the source array.',tasks:['Connect the filter to an input event and show a result count.','Add category filtering that composes with the text query.','Restore both filters with a keyboard-accessible reset control.'],criteria:['Case-insensitive matching and a useful empty state','No HTML injection through user text','Source data remains unchanged after repeated filtering'],question:'Should a no-results search delete items from the source array?',answer:'No. Filtering is a view of the data; clearing the query should restore the original list.'},
 'data-structures-algorithms':{topic:'Trace breadth-first search using a queue',scenario:'A graph represents rooms connected by corridors. Every corridor has the same traversal cost. Find the fewest corridor steps from room A to each reachable room.',explanation:'Breadth-first search visits vertices in increasing distance from the start when every edge has equal weight. Use a FIFO queue and mark a vertex visited when you enqueue it, not after removing it. This prevents duplicate queue entries caused by multiple incoming edges. Maintain a parent map if you need to reconstruct the route.',example:'Edges: A-B, A-C, B-D, C-D, D-E\nStart: queue=[A], distance[A]=0\nVisit A: queue=[B,C], distance[B]=distance[C]=1\nVisit B: queue=[C,D], distance[D]=2\nVisit C: D is already discovered\nVisit D: queue=[E], distance[E]=3',result:'One shortest route to E is A-B-D-E, with three edges. A-C-D-E is equally short. With adjacency lists, the traversal takes O(V + E) time and O(V) auxiliary space.',tasks:['Animate the queue, current vertex and discovered set separately.','Reconstruct a shortest route from the parent map.','Add a disconnected vertex and display an unreachable state.'],criteria:['Each vertex enters the queue at most once','Shortest edge-count route for an unweighted graph','Correct handling of cycles, disconnected nodes and the start vertex'],question:'Does ordinary BFS guarantee the lowest-cost path when edges have different weights?',answer:'No. BFS minimizes edge count; varying edge costs require an algorithm suited to weighted shortest paths.'},
 'ai-engineering':{topic:'Design an evidence-grounded service assistant',scenario:'An internal service team needs answers from approved policy documents and ticket summaries. Some documents are restricted, and some requests ask the system to take an action.',explanation:'Separate retrieval, answer generation and action execution. Apply the requesting user’s permissions before retrieved text reaches the model. Attach source identifiers so an answer can point to its evidence. Treat document instructions as untrusted content. For a state-changing tool, validate the arguments and require the appropriate approval; a generated sentence is not authorization.',example:'Request + user identity\n  -> permission-filtered retrieval\n  -> evidence selection with source IDs\n  -> grounded answer or insufficient-evidence response\n  -> proposed action (if requested)\n  -> schema validation + permission + human approval\n  -> execution receipt + audit event',result:'A useful answer includes evidence and a clear limit when sources disagree or are insufficient. A proposed ticket update remains a proposal until the authorized action boundary approves it. The same architecture can be implemented with Python, .NET, Java or Node.js.',tasks:['Create an evaluation set with answerable, unanswerable and restricted-source requests.','Implement a read-only adapter for an enterprise-system fixture.','Demonstrate a failed retrieval, a denied action and a successful approved action.'],criteria:['No restricted evidence reaches an unauthorized request','Answers cite supporting source IDs and abstain when needed','Tool calls validate inputs and produce auditable execution outcomes'],question:'Why is retrieval quality alone insufficient for a production assistant?',answer:'Permissions, evidence handling, tool authorization, failure recovery and operational monitoring also determine whether the system behaves safely and usefully.'},
 'ai-leadership':{topic:'Choose a pilot with a measurable decision gate',scenario:'A leadership team is comparing an internal knowledge assistant, automated sales outreach and invoice triage. It can fund one small pilot before making a larger commitment.',explanation:'Start with a workflow baseline and a named accountable owner. Compare value, feasibility, data readiness and consequence of error. A high-volume task is not automatically the best first pilot if permissions are unclear or errors are costly. Define the decision to be made at the end of the pilot before buying tools or expanding access.',example:'Pilot: invoice triage, with human approval\nBaseline: record current handling time and correction rate\nScope: one document type and one operational team\nControls: approved data, access limits, escalation owner\nDecision gate: compare measured results with agreed thresholds\nOutcomes: stop, revise scope, or approve controlled expansion',result:'The pilot charter turns an AI idea into a governed experiment. Its success thresholds are agreed by the business and risk owners rather than invented in the course material. A 90-day plan includes discovery, a bounded pilot and an evidence-based scale decision.',tasks:['Prepare a one-page use-case scorecard for three candidate workflows.','Write a pilot charter with owner, baseline, risks and decision gate.','Present a 90-day adoption plan including training and rollback responsibilities.'],criteria:['Explicit business problem and evidence-based baseline','Named control owners and human-review boundaries','A scale/stop decision tied to measured outcomes, not vendor claims'],question:'What should happen when a pilot improves speed but increases consequential errors?',answer:'Review the agreed guardrails and stop or revise the pilot if they are breached; speed alone does not justify expansion.'},
 'applied-ml-engineering':{topic:'Prevent leakage in a customer-risk model',scenario:'A model predicts whether a customer will leave during the next month. The dataset includes activity timestamps and a field populated only after an account is closed.',explanation:'Define the prediction time first. A feature is valid only if it would be available at that time in real use. The post-closure field leaks the target and must not enter training. Split data in a way that reflects deployment, and fit learned preprocessing only on the training partition. Compare a simple baseline before choosing a more complex model.',example:'1. Fix a prediction cutoff for each example.\n2. Exclude features created after that cutoff.\n3. Separate train, validation and final test periods.\n4. Fit preprocessing and the model on training data.\n5. Choose the operating threshold on validation data.\n6. Evaluate once on the held-out test set.',result:'A lower but credible held-out score is more useful than a misleading score caused by leakage. Report the confusion matrix and precision/recall at the chosen threshold, together with subgroup limitations and the cost of false decisions.',tasks:['Document when every candidate feature becomes available.','Build a reproducible baseline pipeline and compare one stronger model.','Prepare a model card with evaluation limits and monitoring signals.'],criteria:['No target or temporal leakage','Reproducible preprocessing and separate final evaluation','Threshold choice explained in terms of the workflow'],question:'Why should the final test set not be used repeatedly to choose model settings?',answer:'Repeated choices adapt the model-selection process to the test set, weakening its value as an independent estimate.'},
 'full-stack':{topic:'Keep form validation and server authority separate',scenario:'A learner submits a project title and repository URL. The interface should give immediate feedback, but the server must remain the authority for stored records.',explanation:'Client validation improves the interaction but can be bypassed. Validate again at the API boundary, derive the owner from the authenticated session and reject unauthorized updates. Use parameterized database access. Treat loading, validation failure, duplicate submission and success as explicit interface states rather than assuming every request succeeds.',example:'POST /projects\nInput: { title, repositoryUrl }\nServer: verify session -> validate input -> authorize owner\n        -> insert record -> return project ID\nUI: idle -> submitting -> success | field error | retry\nNever accept ownerId from the form as proof of ownership.',result:'The page can show field-specific errors without losing the learner’s draft. A successful response identifies the stored project. A network failure should not be displayed as a successful save.',tasks:['Build an accessible form with retained values after a validation error.','Implement API validation and an owner-scoped read/update path.','Test a double click, rejected URL and unauthorized record update.'],criteria:['Server-side validation and ownership enforcement','Accessible loading, error and success states','Tests for invalid input and cross-user access'],question:'Can a hidden ownerId field safely establish who owns a new record?',answer:'No. The server should derive ownership from the authenticated session rather than trusting editable client input.'},
 'data-analytics':{topic:'Find the denominator before drawing a conclusion',scenario:'An enrollment report shows 80 completions from one cohort and 60 from another. A stakeholder concludes that the first cohort performed better.',explanation:'Counts alone do not establish completion rates. Define the eligible cohort, the observation window and the event used to count completion. Deduplicate learners before aggregation. Keep the definition stable across groups, and separate missing data from a genuine non-completion. Explain uncertainty and confounding factors instead of implying the comparison proves a cause.',example:'Illustrative data:\nCohort A: 80 completions / 100 eligible learners = 80%\nCohort B: 60 completions / 60 eligible learners = 100%\nA has more completions; B has the higher completion rate.\nCheck equal follow-up windows before comparing the rates.',result:'A dashboard should show both numerator and denominator, not only a percentage. The example reverses the initial ranking without changing either completion count. It is teaching data, not a claim about academy performance.',tasks:['Write a metric definition including grain, time window and exclusions.','Prepare a deduplicated cohort summary in SQL or a spreadsheet.','Build a chart with sample size, source date and a decision-oriented annotation.'],criteria:['Correct grain and denominator','Reconciled totals with documented exclusions','No causal claim from a descriptive comparison alone'],question:'Why can comparing a new cohort with a fully elapsed cohort be misleading?',answer:'The new cohort has had less time to complete, so unequal observation windows can bias the comparison.'},
 'cybersecurity':{topic:'Trace an access-control failure in a safe lab',scenario:'In an isolated training application, changing a project ID in a URL reveals another test user’s record. The task is to identify the missing control and verify the repair.',explanation:'Authentication establishes identity; authorization determines whether that identity may access a particular object. Enforce the object-level check on every request, including API endpoints that are not directly linked in the UI. Use only the supplied lab accounts and synthetic data. A predictable ID is not the root cause; the absent permission check is.',example:'GET /projects/42\nSession user: learner-A\nRecord owner: learner-B\nRequired decision: deny unless an explicit sharing rule grants access\nRegression checks: owner allowed; other user denied; no session denied\nLog the decision without exposing the private record.',result:'The repaired endpoint evaluates permission before returning the record. Replacing sequential IDs with random IDs may reduce guessing, but it does not replace authorization.',tasks:['Create a small threat model covering actors, assets and trust boundaries.','Write owner/non-owner/no-session regression cases against the local fixture.','Produce a remediation note with scope, evidence and retest results.'],criteria:['Testing stays within the authorized local lab','Permission check occurs before private data is returned','Repair is demonstrated by positive and negative tests'],question:'Does using a UUID instead of an integer remove the need for an ownership check?',answer:'No. Identifier complexity is not a permission boundary.'},
 'space-tech':{topic:'Build a first-pass mission power budget',scenario:'A small satellite concept has an intermittent payload, a continuous onboard computer and a transmitter used during ground contact. The team needs to estimate average electrical demand.',explanation:'Distinguish peak power from orbit-average power. Multiply each operating power by its duty cycle to estimate its average contribution, then add the loads. Energy is power multiplied by time; keep units visible. A first-pass average does not size the battery by itself: eclipse duration, conversion losses, operating constraints and margins also matter.',example:'Illustrative loads:\nPayload: 8 W at 25% duty cycle -> 2 W average\nComputer: 2 W continuously -> 2 W average\nRadio: 10 W at 10% duty cycle -> 1 W average\nTotal before losses and margins: 5 W average\nFor 1 hour, that average corresponds to 5 Wh.',result:'The peak simultaneous load could exceed the average substantially. Document which loads may operate together and evaluate the eclipse case separately. The figures illustrate a method, not a flight-qualified design.',tasks:['Prepare a load table with peak power, duty cycle and operating mode.','Add an eclipse energy case and make assumptions explicit.','Explain a trade-off between payload operation and communication demand.'],criteria:['Consistent W versus Wh units','Peak and average requirements treated separately','Assumptions, losses and margins clearly documented'],question:'Why is average power alone insufficient to choose a power-system component?',answer:'Components must also meet peak loads and operating conditions; energy storage must support the required duration and loss assumptions.'},
 'entrepreneurship':{topic:'Turn an idea into a falsifiable customer hypothesis',scenario:'A founder proposes crop-advisory software for small farmers. Before building the product, the team needs evidence that a specific problem is important enough to change behavior.',explanation:'Separate the customer segment, observed problem and proposed solution. Interviews should explore recent behavior rather than ask people to praise an idea. Record evidence that contradicts the hypothesis as well as evidence that supports it. A mentor gate checks the quality of the evidence and the next experiment; it does not guarantee commercial success or funding.',example:'Hypothesis: a defined farmer segment struggles with a specific decision.\nInterview: ask about the most recent occurrence and current workaround.\nEvidence: record context, frequency, consequence and direct observations.\nSynthesis: group patterns and note contradictions.\nNext test: choose one uncertain assumption and an observable outcome.',result:'The stage artifact is an evidence-backed problem statement, not a feature list. The team may narrow the segment, revise the problem or stop the idea based on what it learns. Consent and privacy govern how interview notes are stored and shared.',tasks:['Draft a segment-specific problem statement and interview guide.','Synthesize interview notes into patterns, contradictions and open questions.','Submit a validation summary and respond to mentor-requested changes.'],criteria:['Behavioral evidence distinguished from opinions','Contradictions and privacy obligations are visible','Next-stage request justified by evidence rather than enthusiasm'],question:'Why is “Would you use my app?” weak validation?',answer:'A hypothetical positive answer does not establish actual behavior, urgency or willingness to change the current workaround.'}
};
export function depthHtml(c,esc,icon){const d=courseDepth[c.slug];return `<section class="card section" id="teaching-example"><div class="cardhead"><div><span class="eyebrow">Inside the course · worked lesson</span><h2>${esc(d.topic)}</h2></div>${icon('BookOpen')}</div><p class="section">${esc(d.scenario)}</p><div class="grid2 section"><div><h3>Understand the concept</h3><p class="section">${esc(d.explanation)}</p><h3 class="section">Interpret the result</h3><p class="section">${esc(d.result)}</p></div><div><h3>Work through the example</h3><pre style="white-space:pre-wrap;overflow-wrap:anywhere;background:#101d3b;color:#e6edff;padding:18px;border-radius:8px;font-size:13px;line-height:1.7;margin-top:12px">${esc(d.example)}</pre><details class="section"><summary>Knowledge check: ${esc(d.question)}</summary><p class="section">${esc(d.answer)}</p></details></div></div><div class="grid2 section"><section class="card"><h3>${icon('Code2')}Your assignment</h3><ol>${d.tasks.map(t=>`<li style="margin:10px 0">${esc(t)}</li>`).join('')}</ol></section><section class="card"><h3>${icon('ClipboardCheck')}What your submission should demonstrate</h3><ul>${d.criteria.map(t=>`<li style="margin:10px 0">${esc(t)}</li>`).join('')}</ul></section></div></section>`;}

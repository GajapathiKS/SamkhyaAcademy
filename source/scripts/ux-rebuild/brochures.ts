import {courseDepth} from './course-depth.mjs';
import {mkdir,readFile,writeFile,copyFile} from 'node:fs/promises';
import path from 'node:path';
import {pathToFileURL} from 'node:url';
import {chromium} from 'playwright';
import {courseCatalog} from '../../packages/catalog/src/index.ts';
import {marketing,courseScreens,brochureName,conceptSvg} from './marketing.mjs';
import {icon,esc} from './render.mjs';
const out=path.resolve('output/UX_REVIEW_ASTRA_2026_09_05'),dir=path.join(out,'brochures');
await mkdir(dir,{recursive:true});await mkdir(path.join(dir,'html'),{recursive:true});
const css=`@page{size:A4 portrait;margin:0}*{box-sizing:border-box}body{margin:0;font:10.5pt/1.55 Poppins,Arial,sans-serif;color:#101b45;background:#e9ecf3}h1,h2,h3,p{margin:0}h1{font-size:31pt;line-height:1.12;letter-spacing:-1.1px}h2{font-size:24pt;line-height:1.2;letter-spacing:-.7px}h3{font-size:12pt;line-height:1.35}p{color:#53617c}.sheet{width:210mm;height:297mm;padding:13mm 14mm 15mm;background:#fff;position:relative;break-after:page;overflow:hidden;display:flex;flex-direction:column;gap:5mm}.sheet:last-child{break-after:auto}.mast{display:flex;justify-content:space-between;align-items:center;padding-bottom:4mm;border-bottom:1px solid #dae0ed;font-size:9pt}.brand{font:700 17pt Georgia,serif;color:#152255}.brand em{font-style:normal;color:#5942ee}.kicker{color:#4d3ad0;font-size:8.5pt;font-weight:600;letter-spacing:1.3px;text-transform:uppercase}.dek{font-size:12pt;line-height:1.6}.cover{background:linear-gradient(145deg,#08132b,#112958);color:#fff}.cover p{color:#cedaee}.cover .brand{color:white}.cover .brand em,.cover .kicker{color:var(--accent)}.cover .mast{border-color:#ffffff24}.cover-image{width:100%;height:89mm;object-fit:cover;border-radius:10px;object-position:center;display:block}.cover h1{font-size:30pt}.headline{font-size:19pt;line-height:1.25;color:var(--accent);font-weight:600}.facts{display:grid;grid-template-columns:repeat(3,1fr);gap:3mm}.fact{border:1px solid #dfe3ef;border-radius:7px;padding:3mm;display:flex;align-items:center;gap:2.5mm;font-size:9pt}.cover .fact{border-color:#ffffff2c;background:#ffffff07}.icon{width:22px;height:22px;flex-shrink:0;color:#5840df;stroke-width:1.6}.cover .icon{color:var(--accent)}.grid2{display:grid;grid-template-columns:1fr 1fr;gap:4mm}.card{border:1px solid #dfe4ef;border-radius:8px;padding:4mm;min-width:0}.card h3{display:flex;align-items:center;gap:2mm;margin-bottom:2mm}.card p,.card li{font-size:9.5pt}.card ul{padding-left:4.5mm;margin:2mm 0 0}.outcomes{display:grid;grid-template-columns:1fr 1fr;gap:3mm}.outcome{display:flex;gap:3mm;padding:4mm;border:1px solid #dfe4ef;border-radius:7px;align-items:flex-start}.outcome p{color:#14204c;font-size:10pt}.diagram{width:100%;height:56mm;object-fit:contain;background:#091a40;border-radius:8px}.module{padding:3.5mm}.module h3{font-size:10.5pt}.module p,.module li{font-size:8.7pt;line-height:1.5}.module:last-child:nth-child(odd){grid-column:1/-1}.module ul{margin:2mm 0 0}.module-number{display:inline-flex;align-items:center;justify-content:center;background:#efedff;color:#513ad6;width:23px;height:23px;border-radius:6px;font-size:9pt;flex-shrink:0}.toolbox{display:flex;gap:2mm;flex-wrap:wrap}.toolbox span{font-size:9pt;border:1px solid #dce1ee;padding:2mm 3mm;border-radius:5px;background:#f7f8fc}.band{background:#f1f5fa;border:1px solid #dce3ef;padding:5mm;border-radius:8px}.band.dark{background:linear-gradient(120deg,#102149,#30229b);color:white}.band.dark p{color:#dbe3f6}.band h3{margin-bottom:2mm}.cta{display:inline-flex;gap:8px;align-items:center;background:#5238e8;color:white;padding:3mm 5mm;border-radius:5px;text-decoration:none;font-weight:600;font-size:10pt}.cover .cta{background:var(--accent);color:#061829}.foot{position:absolute;left:14mm;right:14mm;bottom:6mm;display:flex;justify-content:space-between;font-size:8pt;color:#72809a;border-top:1px solid #dce1ec;padding-top:2mm}.cover .foot{color:#c0cce3;border-color:#ffffff24}.note{font-size:8.5pt;line-height:1.5}.grow{flex:1}.evidence{display:grid;grid-template-columns:repeat(3,1fr);gap:3mm}.evidence>div{padding:4mm;border:1px solid #dfe4ef;border-radius:7px}.evidence h3{font-size:10pt;margin-top:2mm}.step{display:flex;gap:3mm;align-items:flex-start;margin-top:3mm}.step b{width:24px;height:24px;background:#eeeaff;color:#543de1;display:flex;align-items:center;justify-content:center;border-radius:50%;flex-shrink:0}.step p{font-size:10pt}.fine{font-size:8pt;color:#6b7790}a{color:#4c33dd}.cover .grow{display:none}.cover-image{height:auto;min-height:80mm;max-height:135mm;flex:1;object-fit:cover}.cover .headline{color:var(--accent)}.cta .icon{color:inherit}.sheet:nth-child(4){gap:4mm}.sheet:nth-child(4) .dek{font-size:11pt}.sheet:nth-child(4) .card{padding:3mm}.sheet:nth-child(4) .band{padding:4mm}.sheet:nth-child(4) .evidence>div{padding:3mm}.warm.cover{background:linear-gradient(145deg,#fffdf7,#f4ecd9);color:#153c3b}.warm.cover p{color:#455f62}.warm.cover .brand{color:#173e42}.warm.cover .headline{color:#167471}.warm.cover .foot{color:#647b79;border-color:#d7d5c5}.warm.cover .fact{border-color:#d3dacb}.warm.cover .mast{border-color:#d3dacb}.warm.cover .cta{background:#156b66;color:white}@media screen{.sheet{margin:15px auto;box-shadow:0 8px 35px #121c3a18}}`;
await writeFile(path.join(dir,'brochure.css'),css);
const browser=await chromium.launch({headless:true});const page=await browser.newPage();const results=[];
for(const c of courseCatalog){const d=courseDepth[c.slug],m=marketing[c.slug],url=`https://samkhyaacademy.com/courses/${c.slug}`,local=`http://127.0.0.1:4310/pages/${String(courseScreens[c.slug]).padStart(3,'0')}.html`;const mast=`<div class="mast"><div class="brand">Samkhya<em>Academy</em></div><span>PROGRAM GUIDE · 2026</span></div>`;
const foot=(n:number)=>`<footer class="foot"><span>SamkhyaAcademy · ${esc(c.title)}</span><span>${String(n).padStart(2,'0')} / 06</span></footer>`;
const wrap=(n:number,content:string,cover=false)=>`<section class="sheet ${cover?['ai-leadership','entrepreneurship'].includes(c.slug)?'cover warm':'cover':''}" style="--accent:${m.accent}">${mast}${content}${foot(n)}</section>`;
const list=(xs:string[])=>`<ul>${xs.map(v=>`<li>${esc(v)}</li>`).join('')}</ul>`;
const delivery=c.deliveryMode==='SELF_PACED'?'Self-paced online':c.deliveryMode==='HYBRID'?'Hybrid learning':c.deliveryMode==='OFFLINE'?'In-person learning':c.deliveryMode==='COHORT'?'Cohort-based learning':'Live online';
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><title>${esc(c.title)} | SamkhyaAcademy Program Guide</title><meta name="author" content="SamkhyaAcademy"><meta name="description" content="${esc(c.shortDescription)}"><link rel="stylesheet" href="../../assets/fonts.css"><link rel="stylesheet" href="../brochure.css"></head><body>
${wrap(1,`<span class="kicker">${esc(c.category)} · ${esc(c.level)}</span><h1>${esc(c.title)}</h1><p class="headline">${m.headline}</p><img class="cover-image" src="../../assets/course-visuals/${m.image}" alt="${esc(c.title)} program illustration"><p class="dek">${esc(c.shortDescription)}</p><div class="facts"><div class="fact">${icon('Clock3')}${esc(c.durationText)}</div><div class="fact">${icon('Layers')}${c.modules.length} modules</div><div class="fact">${icon(c.pricingCategory==='FREE'?'Gift':'GraduationCap')}${c.pricingCategory==='FREE'?'Free course':'Professional learning'}</div></div><div class="grow"></div><div><a class="cta" href="${local}">Explore the learning experience ${icon('ArrowRight')}</a><p class="note" style="margin-top:3mm">Practical learning. Clear progression. Work you can explain.</p></div>`,true)}
${wrap(2,`<span class="kicker">Your next capability</span><h2>What will you be able to do?</h2><p class="dek">${esc(c.description)}</p><div class="outcomes">${c.outcomes.map((v,i)=>`<div class="outcome">${icon(['Target','Code2','ShieldCheck','Rocket'][i%4])}<p>${esc(v)}</p></div>`).join('')}</div><div class="grid2"><div class="card"><h3>${icon('Users')}Who this is for</h3>${list(c.audience)}</div><div class="card"><h3>${icon('ClipboardCheck')}Your starting point</h3>${list(c.prerequisites)}</div></div><h3>Your progression at a glance</h3><img class="diagram" src="../../assets/course-visuals/${c.slug}-roadmap.svg" alt="${esc(m.stages.join(' to '))}"><div class="band"><h3>Learn it. Apply it. Explain it.</h3><p>Move from guided concepts to practical exercises and a coherent piece of work. Use the course resources to revisit difficult ideas and make your reasoning visible.</p></div>`)}
${wrap(3,`<span class="kicker">The curriculum</span><h2>A clear route through the subject.</h2><p>Every module builds towards practical work. The course outline below is aligned with the academy's current program catalog.</p><div class="grid2">${c.modules.map((mod,i)=>`<article class="card module"><h3><span class="module-number">${i+1}</span>${esc(mod.title)}</h3><p>${esc(mod.description)}</p>${list(mod.lessons)}</article>`).join('')}</div>${c.modules.length<=4?`<img class="diagram" src="../../assets/course-visuals/${c.slug}-roadmap.svg" alt="${esc(m.stages.join(' to '))}">`:'<div class="grow"></div>'}${c.modules.length<7?`<div class="band"><h3>${icon(m.icon)}Practice with purpose</h3><p>${m.brief}</p></div>`:''}`)}
${wrap(4,`<span class="kicker">Build something that demonstrates your learning</span><h2>${m.project}</h2><p class="dek">${m.brief}</p><div class="evidence">${m.evidence.map((v,i)=>`<div>${icon(['FolderCode','ClipboardCheck','Presentation'][i])}<h3>${v}</h3></div>`).join('')}</div><div class="card"><h3>${icon('Wrench')}Tools and working methods</h3><div class="toolbox">${c.tools.map(t=>`<span>${esc(t)}</span>`).join('')}</div><p class="note" style="margin-top:3mm">Tools are part of the learning workflow, not partner endorsements. Examples may evolve while preserving the learning outcomes.</p></div><div class="grid2"><div class="card"><h3>${icon('CalendarDays')}Delivery</h3><p>${delivery} · ${esc(c.durationText)}</p><p style="margin-top:2mm">Confirm the current schedule, delivery arrangements and any prerequisites before enrolling.</p></div><div class="card"><h3>${icon('Award')}Completion</h3><p>${c.certificateEnabled?'A completion certificate is available after the required course work':'Complete the required course work'}${c.requiresPassingExam?' and a passing assessment':''}.</p><p class="note" style="margin-top:2mm">Course completion is not a job, placement or funding guarantee.</p></div></div><div class="band dark"><h3>Your next steps</h3><div class="step"><b>1</b><p>Review the curriculum and your starting point.</p></div><div class="step"><b>2</b><p>${c.pricingCategory==='FREE'?'Create your learner account and enroll for free.':'Review current enrollment options and delivery details.'}</p></div><div class="step"><b>3</b><p>Start learning, keep your evidence and track your progress.</p></div></div><div class="grow"></div><div><a class="cta" href="${local}">Open the course experience ${icon('ArrowRight')}</a><p class="fine" style="margin-top:3mm">Customer-preview edition. Interactive links open the local academy prototype. Confirm course availability, schedule and commercial terms with the academy.</p></div>`)}
${wrap(5,`<span class="kicker">Inside the learning experience</span><h2>${esc(d.topic)}</h2><p class="dek">${esc(d.scenario)}</p><div class="card"><h3>${icon("BookOpen")}Understand the concept</h3><p>${esc(d.explanation)}</p></div><div class="band dark"><h3>Worked example</h3><pre style="white-space:pre-wrap;overflow-wrap:anywhere;font:9.5pt/1.65 monospace;color:#eff4ff">${esc(d.example)}</pre></div><div class="card"><h3>${icon("Search")}Interpret the result</h3><p>${esc(d.result)}</p></div><div class="band"><h3>Knowledge check</h3><p>${esc(d.question)}</p><p style="margin-top:3mm"><b>Explanation:</b> ${esc(d.answer)}</p></div>`)}
${wrap(6,`<span class="kicker">Your practical assignment</span><h2>Build evidence, not just familiarity.</h2><p class="dek">${esc(d.topic)}</p><div class="card"><h3>${icon("ClipboardList")}Your task</h3>${list(d.tasks)}</div><div class="card"><h3>${icon("ClipboardCheck")}Submission review criteria</h3>${list(d.criteria)}</div><div class="grid2"><div class="card"><h3>Submit</h3><p>A working artifact or analysis, a short explanation of your decisions, and evidence that addresses each review criterion.</p></div><div class="card"><h3>Reflect</h3><p>Describe one failed approach, how you diagnosed it, and what you would test next. Identify limitations instead of hiding them.</p></div></div><div class="band dark"><h3>Your project connection</h3><p>${esc(m.brief)}</p><p style="margin-top:3mm">Use the sample assignment as a stepping stone toward the course project, not as a substitute for the full curriculum.</p></div><a class="cta" href="${local}#teaching-example">Explore the worked lesson ${icon("ArrowRight")}</a><p class="fine">Proposed teaching sample for curriculum and UX review. Full lesson production, instructor review and delivery scheduling remain separate work.</p>`)}
</body></html>`;
const file=path.join(dir,'html',`${c.slug}.html`);await writeFile(file,html);await page.goto(pathToFileURL(file).href);await page.evaluate(()=>document.fonts.ready);await page.emulateMedia({media:'print'});
const layout=await page.evaluate(()=>({badImages:[...document.images].filter(x=>!x.complete||!x.naturalWidth).map(x=>x.src),overflow:[...document.querySelectorAll('.sheet')].flatMap((s,i)=>[...s.children].filter(e=>!e.classList.contains('foot')&&e.getBoundingClientRect().bottom>s.getBoundingClientRect().bottom-49).map(e=>({page:i+1,text:e.textContent?.slice(0,100)})))}));
if(layout.badImages.length||layout.overflow.length){console.error(`${c.slug}: ${JSON.stringify(layout)}`);process.exitCode=1;}
const pdf=path.join(dir,brochureName(c));await page.pdf({path:pdf,format:'A4',printBackground:true,preferCSSPageSize:true,tagged:true});
results.push({slug:c.slug,title:c.title,pdf:`brochures/${brochureName(c)}`,pages:6,layout});console.log(`PDF ${c.slug}: 6 pages`);
}
await browser.close();await writeFile(path.join(dir,'manifest.json'),JSON.stringify(results,null,2));
console.log('12 course brochures generated. Run PDF rendering QA before delivery.');

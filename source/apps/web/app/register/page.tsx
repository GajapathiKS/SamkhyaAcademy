'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Register(){const r=useRouter();const[error,setError]=useState('');async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),mobile:f.get('mobile'),password:f.get('password')})});if(res.ok)r.push('/dashboard');else setError('Registration failed')}return <section><div className="shell"><form onSubmit={go} className="card" style={{maxWidth:440,margin:'40px auto'}}><h1>Create account</h1>{['name','email','mobile','password'].map(x=><input key={x} name={x} type={x==='password'?'password':x==='email'?'email':'text'} required={x!=='mobile'} placeholder={x[0].toUpperCase()+x.slice(1)} style={{width:'100%',padding:13,margin:'8px 0'}}/>)}{error&&<p style={{color:'crimson'}}>{error}</p>}<button className="btn primary" style={{width:'100%'}}>Create account</button></form></div></section>}

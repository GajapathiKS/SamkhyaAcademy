import { z } from 'zod'
import { prisma } from '@samkhya/db'
import { requireCrmUser } from '@/lib/auth'
const schema=z.object({status:z.enum(['NEW','CONTACTED','QUALIFIED','COUNSELLING','APPLICATION','PAYMENT_PENDING','ENROLLED','LOST']).optional(),assignedToUserId:z.string().nullable().optional(),nextFollowUpAt:z.string().datetime().nullable().optional(),company:z.string().max(200).nullable().optional(),designation:z.string().max(200).nullable().optional()})
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const admin=await requireCrmUser();const {id}=await params;const parsed=schema.safeParse(await req.json());if(!parsed.success)return Response.json({errors:parsed.error.flatten()},{status:400});const data:any={...parsed.data};if('nextFollowUpAt' in data)data.nextFollowUpAt=data.nextFollowUpAt?new Date(data.nextFollowUpAt):null;const lead=await prisma.lead.update({where:{id},data});await prisma.auditLog.create({data:{actorUserId:admin.id,action:'CRM_LEAD_UPDATED',entityType:'Lead',entityId:id,payload:parsed.data as any}});return Response.json(lead)}catch(e:any){return Response.json({error:String(e?.message||e)},{status:String(e?.message).includes('FORBIDDEN')?403:500})}}

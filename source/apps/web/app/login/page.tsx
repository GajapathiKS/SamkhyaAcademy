'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const r=useRouter();const[error,setError]=useState('');async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(res.ok)r.push('/dashboard');else setError((await res.json()).error||'Login failed')}return <section><div className="shell"><form onSubmit={go} className="card" style={{maxWidth:440,margin:'40px auto'}}><h1>Sign in</h1><p className="muted">Access your courses, exams, progress and certificates.</p><input name="email" type="email" required placeholder="Email" style={{width:'100%',padding:13,margin:'8px 0'}}/><input name="password" type="password" required placeholder="Password" style={{width:'100%',padding:13,margin:'8px 0'}}/>{error&&<p style={{color:'crimson'}}>{error}</p>}<button className="btn primary" style={{width:'100%'}}>Sign in</button></form></div></section>}

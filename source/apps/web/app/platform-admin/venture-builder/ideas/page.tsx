import { prisma } from '@samkhya/db'
import { requireUser } from '@/lib/auth'
export const dynamic='force-dynamic'
export default async function IdeasQueue(){const user=await requireUser();if(!['PLATFORM_ADMIN','CONTENT_ADMIN','VENTURE_MENTOR'].includes(String(user.platformRole)))return <main className="shell"><h1>Forbidden</h1></main>;const ideas=await prisma.ventureIdeaSubmission.findMany({where:{status:{in:['SUBMITTED','SHORTLISTED']}},include:{user:true},orderBy:{submittedAt:'asc'}});return <main className="venture-page"><section className="venture-hero"><div className="venture-shell"><div className="venture-kicker">Opportunity Intake</div><h1>Founder Idea Review Queue</h1><p>Review structured idea submissions before admitting them into the Venture Builder journey.</p></div></section><section><div className="venture-shell"><div className="venture-panel"><div className="table-responsive"><table className="venture-table"><thead><tr><th>Idea</th><th>Founder</th><th>Problem</th><th>Target Customer</th><th>Support Needs</th><th>Status</th></tr></thead><tbody>{ideas.map(i=><tr key={i.id}><td><strong>{i.ventureName}</strong><br/><span className="muted">{i.ideaSummary}</span></td><td>{i.user.name}<br/><span className="muted">{i.user.email}</span></td><td>{i.problemStatement.slice(0,140)}{i.problemStatement.length>140?'…':''}</td><td>{i.targetCustomer}</td><td>{Array.isArray(i.supportNeeds)?(i.supportNeeds as string[]).join(', '):'—'}</td><td><span className="status-badge review">{i.status}</span></td></tr>)}</tbody></table></div>{ideas.length===0&&<p className="muted">No submitted ideas are waiting for review.</p>}</div></div></section></main>}

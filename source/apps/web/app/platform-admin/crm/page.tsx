import Link from 'next/link'
import { prisma } from '@samkhya/db'
import { requireCrmUser } from '@/lib/auth'
const stages=['NEW','CONTACTED','QUALIFIED','COUNSELLING','APPLICATION','PAYMENT_PENDING','ENROLLED','LOST'] as const
export default async function CrmPage(){await requireCrmUser();const [leads,offers,payments]=await Promise.all([prisma.lead.findMany({orderBy:{updatedAt:'desc'},take:100}),prisma.offer.findMany({orderBy:{createdAt:'desc'},take:20}),prisma.payment.findMany({where:{status:'PAID'},orderBy:{paidAt:'desc'},take:20})]);return <main><section className="hero compact"><div className="shell"><span className="pill">MINI CRM</span><h1>Admissions, enquiries & organization sales</h1><p>Track brochure leads, webinar registrations, counselling, private offers, payments and conversion to enrollment.</p></div></section><section><div className="shell"><div className="grid">{stages.slice(0,7).map(s=><div className="card" key={s}><div className="metric">{leads.filter(l=>l.status===s).length}</div><div className="muted">{s.replaceAll('_',' ')}</div></div>)}</div><div className="actions" style={{marginTop:24}}><Link className="btn primary" href="/admin/crm/marketing">WhatsApp & Email Campaigns</Link></div><h2 className="section-title">Lead pipeline</h2><div className="card" style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th align="left">Lead</th><th align="left">Interest</th><th align="left">Status</th><th align="left">Follow-up</th><th></th></tr></thead><tbody>{leads.map(l=><tr key={l.id}><td><strong>{l.name}</strong><br/><span className="muted">{l.email} · {l.mobile}</span></td><td>{l.programSlug||'General'}</td><td>{l.status}</td><td>{l.nextFollowUpAt?.toLocaleString('en-IN')||'—'}</td><td><Link href={`/platform-admin/crm/${l.id}`}>Open →</Link></td></tr>)}</tbody></table></div><div className="grid"><div className="card"><h3>Active/private offers</h3><div className="metric">{offers.filter(o=>o.status==='ACTIVE').length}</div><p className="muted">On-demand discounted URLs generated for leads and organizations.</p></div><div className="card"><h3>Collected via Razorpay</h3><div className="metric">₹{(payments.reduce((a,p)=>a+p.amountPaise,0)/100).toLocaleString('en-IN')}</div><p className="muted">Recorded paid webhook events in this view.</p></div></div></div></section></main>}

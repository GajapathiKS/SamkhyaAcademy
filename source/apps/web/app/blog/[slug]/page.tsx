import config from '@payload-config'
import { getPayload } from 'payload'
import { notFound } from 'next/navigation'
import { BlogRichText } from '@/components/BlogRichText'
import Link from 'next/link'

export const revalidate = 300

export default async function BlogPost({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params
  if (process.env.UX_CAPTURE_MODE === '1') return <main className="insight-article"><header><div><span>AI & AGENTIC SYSTEMS · FIELD GUIDE</span><h1>From AI demo to reliable production system</h1><p>Evaluation, guardrails and observability are not finishing touches. They are the engineering system that makes AI useful, governable and improvable.</p><div><i>SA</i><b>SamkhyaAcademy Engineering Faculty<small>12 min read · Updated September 2026</small></b></div></div><aside><b>IN THIS GUIDE</b><a href="#frame">1. Frame the outcome</a><a href="#evaluate">2. Evaluate behavior</a><a href="#operate">3. Operate responsibly</a><a href="#checklist">4. Production checklist</a></aside></header><div className="article-layout"><article><p className="article-deck">A convincing demo answers “can the model do this once?” A production system must answer a harder set of questions: when does it work, when does it fail, who can act, what evidence is retained, and how will the team improve it?</p><h2 id="frame">Start with the outcome, not the model</h2><p>Define the decision or workflow being improved, the people affected, the acceptable failure modes and the evidence that will count as progress. This prevents architecture from becoming a search for a fashionable use case.</p><div className="article-framework"><span>DISCOVER</span><i>→</i><span>DESIGN</span><i>→</i><span>BUILD</span><i>→</i><span>EVALUATE</span><i>→</i><span>OPERATE</span></div><h2 id="evaluate">Treat evaluation as an engineering loop</h2><p>Combine representative examples, deterministic checks, rubric-based review and production signals. A single average score hides the exact cases that often matter most.</p><pre><header><span>evaluation-policy.ts</span><button>Copy</button></header><code>{`const evaluationPolicy = {\n  dataset: "representative-and-versioned",\n  checks: ["groundedness", "task-success", "safety"],\n  humanReview: "required-for-high-impact-actions",\n};`}</code></pre><div className="article-callout"><i>!</i><div><b>Keep humans accountable</b><p>Human review must be a designed control with clear authority, context and escalation—not a vague promise that someone is “in the loop.”</p></div></div><h2 id="operate">Build observability around decisions</h2><p>Capture model and prompt versions, retrieved context, tool calls, latency, cost, user corrections and downstream outcomes. Avoid logging sensitive content without a defined need and retention policy.</p><div className="article-table"><div><b>Signal</b><b>What it helps answer</b></div>{[['Task completion','Did the workflow produce a useful result?'],['Groundedness','Can important claims be traced to approved evidence?'],['Correction rate','Where are users repeatedly fixing the system?'],['Cost and latency','Can the experience scale within its service target?']].map(x=><div key={x[0]}><strong>{x[0]}</strong><span>{x[1]}</span></div>)}</div><h2 id="checklist">Production readiness checklist</h2><div className="article-checks">{['Outcome and owner are explicit','Representative evaluation set is versioned','Permissions apply before retrieval and action','High-impact actions require governed review','Telemetry excludes unnecessary sensitive data','Rollback and incident response are rehearsed'].map(x=><p key={x}>✓ {x}</p>)}</div><div className="article-next"><div><span>CONNECTED PROGRAM</span><h3>Forward Deployed AI Engineering</h3><p>Build the technical and customer-facing capability to deploy AI systems in real enterprise workflows.</p></div><Link href="/courses/ai-engineering">Explore the program →</Link></div></article><aside><div><b>Download the checklist</b><p>A one-page production readiness reference for project reviews.</p><button>Download PDF ↓</button></div><div><b>Continue learning</b><a href="/learning-studio/text-lesson?preview=1">Read a text lesson →</a><a href="/learning-studio/code-lab?preview=1">Open an interactive lab →</a><a href="/webinars">Join a live webinar →</a></div><div><b>Share this guide</b><div><button>in</button><button>𝕏</button><button>↗</button></div></div></aside></div></main>
  const payload = await getPayload({ config })
  const result = await payload.find({ collection: 'posts', where: { and: [{ slug: { equals: slug } }, { _status: { equals: 'published' } }] }, depth: 2, limit: 1, overrideAccess: false })
  const post:any = result.docs[0]
  if (!post) notFound()
  return <main><article className="shell article-shell"><span className="pill">{post.category}</span><h1>{post.title}</h1><p className="lead">{post.excerpt}</p>{post.heroImage && typeof post.heroImage==='object' && <img className="article-hero" src={post.heroImage.url} alt={post.heroImage.alt || post.title}/>}<BlogRichText data={post.content}/></article></main>
}
